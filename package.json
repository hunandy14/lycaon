{
  "name": "lycaon",
  "private": true,
  "version": "0.1.0",
  "description": "狼人殺 GM 輔助系統",
  "workspaces": [
    "engine",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "test": "vitest run",
    "build": "npm run build -w engine && npm run build -w client && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "better-sqlite3@11.10.0": true
  }
}
